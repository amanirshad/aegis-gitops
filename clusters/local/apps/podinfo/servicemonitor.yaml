apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: podinfo
  namespace: apps

  labels:
    release: prometheus

spec:
  selector:
    matchLabels:
      app: podinfo

  endpoints:
    - port: http
      path: /metrics
      interval: 30s